# Example site

This directory is the deployable demonstration site for LeanBlog. The framework itself lives in
`src/`, `cli/`, and `tools/`; the neutral framework fixtures live under `test/fixtures/`.

Replace or remove `site/posts/` when using this repository as a template. The build command accepts
any Markdown file or directory, so a different content root does not require framework changes.
